{
  "name": "${name}",
  "version": "${version}",
  "description": "${description}",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "efit release -l -r ./src -c -u -d ./lib --no-color",
    "prepublish": "npm run build"
  },
  "files": [
    "lib"
  ],
  "author": "${author}",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/efitjs/efit.git"
  },
  "homepage": "https://github.com/efitjs/efit",
  "license": "MIT"
}
